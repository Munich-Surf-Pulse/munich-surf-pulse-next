import { DayOfWeek } from "@/components/votes/types";

export const DayOfWeekMap = new Map<number, DayOfWeek>([
  [1, DayOfWeek.SUNDAY],
  [2, DayOfWeek.MONDAY],
  [3, DayOfWeek.TUESDAY],
  [4, DayOfWeek.WEDNESDAY],
  [5, DayOfWeek.THURSDAY],
  [6, DayOfWeek.FRIDAY],
  [7, DayOfWeek.SATURDAY],
]);
